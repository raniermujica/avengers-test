// import { Pipe, PipeTransform } from "@angular/core";


// @Pipe({name: 'heroFilter'})

// export class HeroFilterPipe implements PipeTransform {
   
//     transform(items: any[], searchHeroes: string): any[] {
//        if(!items) return [];
//        if(!searchHeroes) return items;

//        searchHeroes = searchHeroes.toLocaleLowerCase();

//        return items.filter((item:any) => {
//         return items.name.toLocaleLowerCase().includes(searchHeroes);
//        })
//    }

// }